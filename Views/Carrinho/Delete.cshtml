@model Academia.Models.Carrinho

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Deletar</h2>

<h3>Tem certeza que deseja deletar isto?</h3>
<div>
    <h4>Carrinho</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Pessoas.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Pessoas.Email)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Produtos.nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Produtos.nome)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Delete" class="btn btn-default" /> |
    @Html.ActionLink("Voltar", "Index", new { }, htmlAttributes: new { @class = "btn btn-primary " })
</div>
    }
</div>
